<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!--  Splash Screen (android12 이전은 SplashScreen API 지원하지 않으므로 별도의 처리 필요) -->
    <style name="AppTheme.SplashScreen" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash</item>
    </style>

    <!-- TabTheme  -->
    <style name="TabTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">#4f953b</item>                       <!-- 배경 -->
        <item name="android:textColorPrimary">@color/rifle_green</item>     <!-- 탭바 선택된 텍스트 -->
        <item name="android:textColorSecondary">@color/philippine_gray</item>       <!-- 탭바 텍스트 -->
        <item name="colorAccent">@color/rifle_green</item>                  <!-- 하단 움직이는 바 -->
        <item name="android:textStyle">bold</item>
    </style>

    <!-- TabTextBold  -->

    <style name="TabTextBold" parent="TextAppearance.Design.Tab">
        <!--            <item name="android:textSize">16sp</item>-->
        <item name="android:textStyle">bold</item>
    </style>

</resources>